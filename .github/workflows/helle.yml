name: kowmi

on: push

jobs:
  first_job:
    runs-on:  ubuntu-latest

    steps:
      - name: print a message
        run: echo "hello github action"

      - name: welcome massage
        run: echo "welcome massge"

      - name: list file
        run: ls -al

      - name: read file
        run: cat README.md
  
